ENV=development

DATABASE_URL=postgresql://opennow:opennow@localhost:5432/opennow

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# HMAC-SHA256 key for hashing phone numbers in sessions/events (rotate yearly).
PHONE_HASH_SECRET=

# Fernet key for encrypting alert_subscription phone numbers. Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ALERT_ENCRYPTION_KEY=

SENTRY_DSN=
